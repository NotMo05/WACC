# ensures that pairs can be unpacked directly from arrays

# Output:
# 3
#

# Program:

begin
  import "import.wacc"
  int x = 200 + 300;
  int y = x + 400;
  int z = x + y;
  println(x);
  println(y);
  println(z)
end
