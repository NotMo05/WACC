# ensures that pairs can be unpacked directly from arrays

# Output:
# 3
#

# Program:

begin
  import "/home/adiprabs/server_projects/WACC_06/src/test/wacc/wacc-examples/valid/function/simple_functions/functionSimpleLoop.wacc";
  int a = call f(1, 2);
  println(a)
end
