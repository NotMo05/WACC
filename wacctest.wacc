# ensures that pairs can be unpacked directly from arrays

# Output:
# 3
#

# Program:

begin

  int inc(int x) is
    return x + 1
  end
  # import "/home/adiprabs/server_projects/WACC_06/src/test/wacc/wacc-examples/valid/function/simple_functions/incFunction.wacc"
  int a = call inc(2);
  println(a)
end
